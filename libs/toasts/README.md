# toasts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build toasts` to build the library.

## Running unit tests

Run `nx test toasts` to execute the unit tests via [Vitest](https://vitest.dev/).
