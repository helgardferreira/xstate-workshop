# actors

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build actors` to build the library.

## Running unit tests

Run `nx test actors` to execute the unit tests via [Vitest](https://vitest.dev/).
